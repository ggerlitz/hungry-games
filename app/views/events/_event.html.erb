<p><%= event.users.size %> <%= link_to 'foodees', event %> are coming on <%= event.start.strftime("%b. %d at %l:%M%P") %> 
	<% if current_user and event.users.exclude?(current_user) %>
	| <%= link_to 'join', join_event_path(event), method: :post %> </p>
	<% end %>